{
  "name": "zstrategy-executor",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "execute": "ts-node src/execute-ticket.ts",
    "test": "node --test -r ts-node/register src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  }
}
